mutation UpdateAddress(
    $token: String!,
    $addressId: ID!,
    $firstName: String!
    $lastName: String!
    $address1: String!
    $city: String!
    $country: String
    ) {
    customerAddressUpdate(
        customerAccessToken: $token,
        id: $addressId,
        address: {
            firstName: $firstName
            lastName: $lastName
            address1: $address1
            address2: "egypt"
            city: $city
            country: $country
            phone: "010000"
            province: $city
            zip: "123"
        }
    ) {
        customerAddress {
            id
            address1
            city
            country
        }
        customerUserErrors {
            field
            message
        }
    }
}
